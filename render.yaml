services:
  - type: web
    name: bissmoi-laravel
    env: php
    buildCommand: |
      composer install
      php artisan key:generate
      npm install
      npm run build
    startCommand: php artisan serve --host 0.0.0.0 --port 10000
    plan: free
